interface:
  display_name: "TOB Weekly Review"
  short_description: "生成 TOB 研发周报，先回看上周 action，再收敛本周和下周待办"
  default_prompt: "Use $tob-weekly-review to first review last week's next actions with human confirmation, then summarize this week's TOB development progress, risks, and human-confirmed next actions."

policy:
  allow_implicit_invocation: true
